-- Auto-generated Database Schema
-- Project: {{ project_name | default("app") }}
-- Generated by Asset-Generator Tool

{% for table in tables %}
CREATE TABLE IF NOT EXISTS {{ table.name }} (
    id SERIAL PRIMARY KEY,
{% for col in table.columns %}
    {{ col.name }} {{ col.type | upper }}{% if col.nullable is defined and not col.nullable %} NOT NULL{% endif %}{% if col.default is defined %} DEFAULT {{ col.default }}{% endif %}{% if not loop.last %},{% endif %}

{% endfor %}
{% if table.timestamps | default(true) %}
    ,created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
    ,updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
{% endif %}
);

{% endfor %}
{% if foreign_keys is defined %}
-- Foreign Keys
{% for fk in foreign_keys %}
ALTER TABLE {{ fk.table }} ADD CONSTRAINT fk_{{ fk.table }}_{{ fk.column }}
    FOREIGN KEY ({{ fk.column }}) REFERENCES {{ fk.references_table }}({{ fk.references_column | default("id") }});
{% endfor %}
{% endif %}
